package com.keith.inheritancechallenge;

public class Car extends Vehicle {

	public Car(String isSteeringDirection, int currentGear, int moveSpeed) {
		super(isSteeringDirection, currentGear, moveSpeed);
		
	}
}
